<!--
Captiva Tecnología Digital
by: Christian Estrella
date: January 2013
-->

<style type="text/css">
	body {
		padding-top: 40px;
		padding-bottom: 40px;
		background-color: #f5f5f5;
	}

	.form-signin {
		max-width: 400px;
		padding: 19px 29px 29px;
		margin: 0 auto 20px;
		background-color: #fff;
		border: 1px solid #e5e5e5;
		-webkit-border-radius: 5px;
		-moz-border-radius: 5px;
		border-radius: 5px;
		-webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
		-moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
		box-shadow: 0 1px 2px rgba(0,0,0,.05);
	}
	.form-signin .form-signin-heading, .form-signin .checkbox {
		margin-bottom: 10px;
	}
	.form-signin input[type="text"], .form-signin input[type="password"] {
		font-size: 16px;
		height: auto;
		margin-bottom: 15px;
		padding: 7px 9px;
	}
</style>

<div class="container">
	<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :class => 'form-signin'}) do |f| %>
	
	<h2 class="form-signin-heading"><%= t '.signup_title' %></h2>
	
	<%= devise_error_messages! %>

	<div>
		<%= f.label :email %>
		<%= f.email_field :email, :autofocus => true, :class => 'input-xlarge' %>
	</div>

	<div>
		<%= f.label :password, t('.password_label') %>
		<%= f.password_field :password %>
	</div>

	<div>
		<%= f.label :password_confirmation, t('.password_confirmation_label') %>
		<%= f.password_field :password_confirmation %>
	</div>
	
	<% resource.build_company %>
	<%= f.fields_for :company do |builder| %>
		<div>
			<%= builder.label :name, t('.company_label') %>
			<%= builder.text_field :name, :id => 'search_company', data: { autocomplete_source: search_company_by_name_autocompletes_path } %>
			<%= f.hidden_field :company_id, :id => 'search_company_id' %>
		</div>
	<% end %>

	<div>
		<%= f.submit t('.signup_button'), :class => 'btn btn-primary' %>
	</div>
	
	<br />
	<%= render "devise/shared/links" %>
	<% end %>
</div>